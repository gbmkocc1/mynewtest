name: my first workflow

on: push

jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v4

    - name: welcome message
        run: |
        echo "My first Github Action"
        ls -ltra
        cat README.md

    - name: Generate ASCII artwork
        run: cowsay -f dragon "RUN for cover,I'm a dragon"

    # - name: List files
    #   run: ls

    # - name: Read file
    #   run: cat README.md